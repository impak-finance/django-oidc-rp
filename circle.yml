machine:
  python:
    version: 3.6.1

dependencies:
  pre:
    - pip install pipenv
    - pip install --upgrade setuptools
    - pipenv install --dev

test:
  override:
    - make qa
    - make tests
  post:
    - pipenv run py.test --cov-report html:$CIRCLE_ARTIFACTS --cov oidc_rp
